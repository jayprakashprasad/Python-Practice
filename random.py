# Online Python compiler (interpreter) to run Python online.
# Write Python 3 code in this online editor and run it.

"""from  random import random
class RandomSequence:
     def __init__(self,container):
       self.sequence=[]
       for item in container:
         if random()<0.5:
           self.sequence.append(item)
          # print("len is called")
          # prinnt( len(self.sequence))
     def __len__(self):
       print("len is called")
       return len(self.sequence)

d=["a",'b','c','d','e']
rs=RandomSequence(d)
print(len(rs))


class ReverseAll:
     def __init__(self,data):
       self.elements=[]
       for element in data:
          revers=element[::-1]
          self.elements.append(revers)
        
     def __str__ (self):
      return f"{self.elements}"

d=["all",'ball','call','eat','eat']
rs=ReverseAll(d)
print((rs))

da=["all",'helo','whatsup']
print(ReverseAll(da[2]))
print(ReverseAll(da))


















####
import turtle

t=turtle.Turtle()
t.setheading(180)
t.forward(200)
t.left(10)"""


